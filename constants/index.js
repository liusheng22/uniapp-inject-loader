const DATA_TYPES = ['data', 'props', 'computed', 'methods', 'watch', 'emits']

const OBJECT_PROPERTIES = ['props', 'computed', 'methods', 'watch']

const LIFECYCLE_HOOKS = [
  'beforeCreate',
  'created',
  'beforeMount',
  'mounted',
  'beforeUpdate',
  'updated',
  'beforeDestroy',
  'destroyed'
]

module.exports = {
  DATA_TYPES,
  OBJECT_PROPERTIES,
  LIFECYCLE_HOOKS
}
